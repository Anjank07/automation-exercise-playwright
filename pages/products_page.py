"""ProductsPage: the /products listing page."""
from pages.base_page import BasePage


class ProductsPage(BasePage):
    """
    Deliberately minimal right now — just enough to prove navigation landed
    in the right place. Search, filters, and "Add to cart" locators get
    added in the next phase, once we're extending real test coverage rather
    than proving the skeleton works end to end.
    """

    pass

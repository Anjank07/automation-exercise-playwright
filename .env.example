# Copy this file to .env and adjust if needed.
# BASE_URL is deliberately NOT here — it lives in pytest.ini's `base_url`
# setting, which is the idiomatic pytest-playwright way to configure the
# target site. This file only holds the handful of things pytest-playwright
# doesn't already manage for you.

DEFAULT_TIMEOUT_MS=10000
